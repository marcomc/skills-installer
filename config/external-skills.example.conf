# skills-installer config.
# Format: TOML.

canonical_dir = "~/.agents/skills"
installer_agent = "codex"
canonical_mode = "copy"
default_agents = [
  "codex",
  "claude-code",
  "openclaw",
]
full_depth = true

[agent_targets]
codex = "~/.codex/skills"
claude-code = "~/.claude/skills"
openclaw = "~/.openclaw/skills"
github-copilot = "~/.copilot/skills"
gemini-cli = "~/.gemini/skills"
opencode = "~/.config/opencode/skills"
antigravity = "~/.gemini/antigravity/skills"
antigravity-cli = "~/.gemini/antigravity-cli/skills"

[[sources]]
name = "matt-pocock"
source = "mattpocock/skills"
canonical_mode = "copy"
include = ["*"]
exclude = ["personal", "in-progress", "deprecated"]
exclude_paths = ["personal", "in-progress", "deprecated"]
agents = [
  "codex",
  "claude-code",
  "openclaw",
]
full_depth = true

# [[sources]]
# name = "marcomc-skills"
# source = "marcomc/skills"
# canonical_mode = "copy"
# include = ["*"]
# exclude = ["in-progress", "deprecated"]
# exclude_paths = ["in-progress", "deprecated"]
# agents = [
#   "codex",
#   "claude-code",
#   "openclaw",
# ]
# full_depth = true
#
# [[sources]]
# name = "local-skills-dev"
# source = "/path/to/local/skills"
# canonical_mode = "symlink"
# include = ["*"]
# exclude = ["personal", "in-progress", "deprecated"]
# exclude_paths = ["personal", "in-progress", "deprecated"]
# agents = ["codex", "claude-code", "openclaw"]
# full_depth = true
